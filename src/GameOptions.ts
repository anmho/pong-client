// pallet size
// ball size

const PALLET_WIDTH = 10;
const PALLET_HEIGHT = 50;
const PALLET_SPEED = 5;

const PalletOptions = {
  PALLET_WIDTH,
  PALLET_HEIGHT,
  PALLET_SPEED,
};

const BALL_RADIUS = 10;
const BALL_SPEED = 5;

const BallOptions = {
  BALL_RADIUS,
  BALL_SPEED,
};

export { PalletOptions, BallOptions };
